export enum TransactionSection {
  TRANSACTION = 'TRANSACTION',
  LEND = 'LEND',
  SPLIT = 'SPLIT',
}

export enum PersonalFinanceSection {
  EXPENSE = 'EXPENSE',
  INCOME = 'INCOME',
  TRANSFER = 'TRANSFER',
  INVESTMENT = 'INVESTMENT',
}

export enum SyncStatus {
  PENDING = 'pending',
  SYNCED = 'synced',
}
